## first of all write 
`git init  `
## then add the readme.md file 
`git add README.md`
## then commi with a massage
`git commit -m "this is a test commit   `
## then we sholud change the branch name from master to main
`git branch -M main`
## then we apply the remote jisse hamre git repo ko pata chal sake ki hamre files ko kaha remotly push karna hai 
`git remote add origin https://github.com/stomstunner/Doller.git`

## now we set the upstream 
`git push -u origin main`